import { TargetAccountValidationDirective } from './target-account-validation.directive';

describe('TargetAccountValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new TargetAccountValidationDirective();
    expect(directive).toBeTruthy();
  });
});
